const navbarStyle = (theme) => ({
  root: {
    flexGrow: 1,
  },

  AppBar: {
    padding: 2,
    background: "transparent",
    boxShadow: "none",
    opacity: 0.9,
  },


  title: {
      flexGrow: 1,
  },

  menuButton: {
    marginRight: theme.spacing(2),
  },

});

export default navbarStyle;
